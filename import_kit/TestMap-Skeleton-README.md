# Procedural Test-Map Skeleton

A **from-scratch** generated map, as opposed to the DM-Rankin replication kit
in this folder. Generated by [`gen_testmap.py`](../gen_testmap.py) — a
minimal exercise of the "write side" of the pipeline (CLAUDE.md): plain-text
T3D in, UnrealEd does CSG/lighting/paths, real `.ut2` out.

## What's in `TestMap-Skeleton.t3d`

A single square room, 1536x1536x384uu (floor at Z=0, ceiling at Z=384), plus:

| Actor(s) | Count | Purpose |
|---|---:|---|
| `Brush` (`CSG_Subtract`) | 1 | Room shell — hollows the level into a box room |
| `Brush` (`CSG_Add`) | 1 | 256x256x64 platform in the centre — visual landmark + jump obstacle |
| `Light` | 5 | One centred + one per quadrant, near the ceiling |
| `PlayerStart` | 4 | One per corner, facing the centre |
| `PathNode` | 4 | Wall midpoints, linking corners to the centre |
| `MiniHealthPack` + `InventorySpot` | 1 pair | Health |
| `ShockAmmoPickup` + `InventorySpot` | 1 pair | Ammo |
| `AdrenalinePickup` + `InventorySpot` | 1 pair | Adrenaline |
| `xWeaponBase` + `InventorySpot` | 1 pair | Weapon (ShockRifle charger) |

Each pickup has its own `InventorySpot` floor-anchored ~16uu below it
(the 1:1 pickup<->InventorySpot rule from `NAV_PATTERNS.md`). All
brush/pickup-to-spot cross references use `MyLevel.<Name>` qualification —
the convention an unsaved level's package uses (see PROGRESS.md Step 7
lessons).

The weapon is placed as an `xWeaponBase` "charger" (`StaticMesh`-driven,
`WeaponType=Class'XWeapons.ShockRifle'`) — the same pattern every stock DM map
uses, and the `InventorySpot` links back via `myPickupBase` (not
`markedItem`, which is for plain `Pickup` subclasses). **Do not** place a bare
`Weapon` subclass (e.g. `ShockRifle`) directly: its skeletal view-mesh crashes
UnrealEd's viewport renderer (`USkeletalMeshInstance::Render` GPF) when
rendered outside of normal pawn-spawn initialization — this was caught when
v1 of this kit was pasted into UnrealEd.

No `Texture=` is set on any brush face, so surfaces come in with the default
checker texture — easy to spot and to retexture by hand afterwards.

## Import steps

1. **Launch UnrealEd** (`C:\UT2004\System\UnrealEd.exe` or `UT2004.exe editor`)
   on a blank `Untitled` level (**File -> New** if not already empty).
2. Open `TestMap-Skeleton.t3d` in a text editor, **Ctrl+A / Ctrl+C**.
3. In UnrealEd: **Edit -> Paste** (Ctrl+V). The log should report
   **23 actors pasted**, with no "object not found" warnings (the kit is
   self-contained — no external asset references).

## Build steps

Run these from the **Build** menu (or **Build All**), in order:

1. **Build Geometry** — processes the two pasted `Brush` actors in CSG order
   and rebuilds the level `Model`: you should see a hollow room with a
   raised platform in the middle.
2. **Build Lighting** — bakes the 5 `Light` actors onto the new BSP surfaces.
3. **Build Paths** (AI Paths) — connects the `PathNode`/`InventorySpot`/
   `PlayerStart` nodes with `ReachSpec` edges (per `NAV_PATTERNS.md`, expect
   roughly out-degree ~4 per node, mostly walk edges since everything here
   is on one flat floor).

## Verification checklist

- [ ] **Paste**: 23 actors, no red error lines.
- [ ] **Build Geometry**: room is hollow (walk inside it in a 3D viewport);
      the centre platform is a solid 256x256x64 box you can walk onto.
- [ ] **Build Lighting**: room is lit (not pitch black) after the bake.
- [ ] **Build Paths**: `Tools -> Path Builder` (or the log from Build Paths)
      shows ReachSpecs connecting all 4 `PlayerStart`s, 4 `PathNode`s, and
      4 `InventorySpot`s — no isolated/orphaned nodes.
- [ ] **Pickups**: all 4 pickups render at their spots, ~16uu above the
      floor, directly above their `InventorySpot`.
- [ ] **Play**: `Play This Level` — bots spawn at the `PlayerStart`s, path
      around the room and onto the platform, and pick up items.
- [ ] **Save As** a new `.ut2` (e.g. `DM-TestSkeleton.ut2`) once everything
      above looks right.

## Regenerate / customize

```sh
python3 gen_testmap.py -o import_kit/TestMap-Skeleton.t3d
python3 gen_testmap.py --room-xy 1024 --room-h 512 -o import_kit/TestMap-Skeleton.t3d
```

`--room-xy` is the room's half-extent on X/Y (default 768 -> 1536x1536);
`--room-h` is its height (default 384). Everything else (platform, lights,
spawns, pickups, path nodes) scales/repositions relative to the room size.

## What this does NOT cover yet

- Only one room / one CSG_Add shape — no corridors, doors, lifts, or
  multi-room layouts (see the "lift template" in `NAV_PATTERNS.md` for the
  next parametric piece worth generating).
- No textures on brush faces (deliberate — default checker for visibility).
- Bot pathing is left entirely to **Build Paths**; we don't author
  `ReachSpec`s by hand (consistent with Step 7/8 findings: the engine
  regenerates these reliably from node placement).
